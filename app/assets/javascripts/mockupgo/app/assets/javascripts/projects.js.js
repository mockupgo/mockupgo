//@ sourceMappingURL=mockupgo\app\assets\javascripts\mockupgo\app\assets\javascripts\projects.js.map
// Generated by CoffeeScript 1.6.1
(function() {

  jQuery(function() {
    return $('#new_page').fileupload({
      dataType: "script",
      add: function(e, data) {
        var file, types, x;
        types = /(\.|\/)(gif|jpe?g|png)$/i;
        file = data.files[0];
        if (types.test(file.type) || types.test(file.name)) {
          x = tmpl("template-upload", file);
          data.context = $(tmpl("template-upload", file));
          $('#new_page').append(data.context);
          return data.submit();
        } else {
          return alert("" + file.name + " is not a gif, jpeg, or png image file");
        }
      },
      progress: function(e, data) {
        var progress;
        if (data.context) {
          progress = parseInt(data.loaded / data.total * 100, 10);
          return data.context.find('.bar').css('width', progress + '%');
        }
      },
      done: function(e, data) {
        $('.no-project').hide();
        if (data.context) {
          return data.context.remove();
        }
      }
    });
  });

}).call(this);
