.navigation
  =link_to dashboard_url, :class => "" do 
    %i{class: "icon-home"}
    Dashboard

%h1.project

  = @project.name


  = link_to edit_project_path(@project), class: "btn btn-mini" do
    %i{class: "icon-edit"}
    Manage project


%ul#images.clearfix
- if @project.pages.length > 0
  = render @project.pages
- else
  %p.no-project
    You don't have any image mockup yet. 
    %br/
    Drag and drop jpeg or png files ore use the box below to upload one.


.well.clearfix
  %h4 Import new mockups
  = form_for [@project, @project.pages.build], :html => {:multipart => true} do |f|
    = f.file_field :image_versions, multiple: true, name: "image_version[image]"
    = f.submit "Import"



<script id="template-upload" type="text/x-tmpl">
<div class="upload">
{%=o.name%}
<div class="progress"><div class="bar" style="width: 0%"></div></div>
</div>
</script>

