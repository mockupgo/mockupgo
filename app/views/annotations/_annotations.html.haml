- cnt = 0
- annotations.each do |note|
  .note{"data-id" => note.id, :style => "top: #{note.top}px; left: #{note.left}px; width: #{note.width}px; height: #{note.height}px; "}
    %span.black.note-counter
      = cnt += 1
    %a.delete-note{:href => "#", :style => "display: block;"} Delete
    .note-wrapper
      .note-comment
        %span#arrow
        .comment
          .comment-author
            bob@bob.com (6h agos):
          .comment-text= note.comment
        .comment
          .comment-author
            bob@bob.com (6h agos):
          .comment-text= note.comment
        .comment
          %btn.btn.btn-small
            reply