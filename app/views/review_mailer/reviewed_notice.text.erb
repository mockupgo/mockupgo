Hi,

<%= @reviewer.email %> has marked the mockup for version <%= @image_version.version_number %> of '<%= @image_version.page.name %>' as reviewed.

<%= @note.to_s %>

You can consult it here:
<%= preview_image_version_url(@image_version) %>

Regards


