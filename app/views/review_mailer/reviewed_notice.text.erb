Hi,

<%= @reviewer.email %> has marked the mockup for '<%= @image_version.page.name %>' as reviewed.

<%= @note.to_s %>

You can consult it here:
<%= preview_image_version_url(@image_version) %>

Regards


